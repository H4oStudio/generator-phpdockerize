<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="opa_data.list">
      <field name="name">opa_data list</field>
      <field name="model">opa_data.opa_data</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="opa_data.action_window">
      <field name="name">opa_data window</field>
      <field name="res_model">opa_data.opa_data</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="opa_data.action_server">
      <field name="name">opa_data server</field>
      <field name="model_id" ref="model_opa_data_opa_data"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="opa_data" id="opa_data.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="opa_data.menu_1" parent="opa_data.menu_root"/>
    <menuitem name="Menu 2" id="opa_data.menu_2" parent="opa_data.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="opa_data.menu_1_list" parent="opa_data.menu_1"
              action="opa_data.action_window"/>
    <menuitem name="Server to list" id="opa_data" parent="opa_data.menu_2"
              action="opa_data.action_server"/>
    -->
  </data>
</odoo>